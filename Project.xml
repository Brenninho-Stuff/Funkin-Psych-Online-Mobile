<?xml version="1.0" encoding="utf-8"?>
<project>
 
	<!-- _________________________ Application Settings _________________________ -->
 
	<app title="Friday Night Funkin': Psych Online" file="PsychOnline" main="Main"
		packageName="boo.sniro.psychonline" package="boo.sniro.psychonline"
		company="Snirozu" version="0.14.6" />
 
	<app if="mobile" title="FNF: Psych Online" />
 
	<!-- Switch Export with Unique ApplicationID and Icon -->
	<set name="APP_ID" value="0x0100f6c013bbc000" />
 
	<!-- NOTE TO SELF: DISABLE THIS IF ISSUES ARE FOUND -->
	<haxedef name="LINC_LUA_RELATIVE_DYNAMIC_LIB" />
 
	<app preloader="flixel.system.FlxPreloader" />
 
	<!-- Minimum without FLX_NO_GAMEPAD: 11.8, without FLX_NO_NATIVE_CURSOR: 11.2 -->
	<set name="SWF_VERSION" value="11.8" />
 
	<!-- _____________________ PSYCH ENGINE CUSTOMIZATION _____________________ -->
 
	<define name="MODS_ALLOWED" />
	<define name="HSCRIPT_ALLOWED" />
	<define name="LUA_ALLOWED" if="cpp" />
	<define name="ACHIEVEMENTS_ALLOWED" />
	<define name="DISCORD_ALLOWED" if="desktop" unless="hl" />
	<define name="VIDEOS_ALLOWED" if="desktop || mobile" />
	<define name="PSYCH_WATERMARKS" />
	<define name="TITLE_SCREEN_EASTER_EGG" />
	<define name="RAR_SUPPORTED" if="windows" unless="hl" />
 
	<!--
		MODS_DATA_FOLDER: on mobile, mods are NOT bundled in the APK.
		Instead they are read from the app's external data directory:
		  Android: /sdcard/Android/data/boo.sniro.psychonline/files/mods/
		In your Haxe code (e.g. Mods.hx or wherever you set the mods folder path),
		add this check:
 
		  #if (mobile && MODS_DATA_FOLDER)
		  var modsPath = lime.system.System.applicationStorageDirectory + "mods/";
		  #else
		  var modsPath = "mods/";
		  #end
 
		Users place mods in that external folder manually.
		On Android 10+, READ/WRITE_EXTERNAL_STORAGE permissions below cover it.
	-->
	<define name="MODS_DATA_FOLDER" if="mobile" />
 
	<!-- __________________________ Android SDK Targets _________________________ -->
	<!--
		minSdkVersion 21  = Android 5.0  (broad device support)
		targetSdkVersion 33 = Android 13 (required for Play Store, safe for sideload)
	-->
	<section if="android">
		<set name="ANDROID_TARGET_SDK_VERSION" value="33" />
		<set name="ANDROID_MIN_SDK_VERSION"    value="21" />
 
		<!-- Internet (required for Colyseus online multiplayer) -->
		<android permission="android.permission.INTERNET" />
 
		<!-- Network state (used by Colyseus to detect connectivity) -->
		<android permission="android.permission.ACCESS_NETWORK_STATE" />
 
		<!--
			Storage permissions for reading mods from external data folder.
			maxSdkVersion caps keep us compliant on Android 10+ scoped storage.
			On Android 10+ (API 29+) the app data dir is accessible without these,
			but they are needed if you ever want to read from /sdcard root.
		-->
		<android permission="android.permission.READ_EXTERNAL_STORAGE"  android:maxSdkVersion="32" />
		<android permission="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
 
		<!-- Vibration (optional: used for note hit feedback on mobile) -->
		<android permission="android.permission.VIBRATE" />
	</section>
 
	<!-- __________________________ Window Settings __________________________ -->
 
	<!-- Base: all targets -->
	<window width="1280" height="720" fps="120" background="#000000" hardware="true" vsync="false" />
 
	<!-- HTML5 -->
	<window if="html5" resizable="true" />
 
	<!-- Desktop -->
	<window if="desktop" orientation="landscape" fullscreen="false" resizable="true" vsync="false" />
 
	<!--
		Mobile: width/height 0 = use native screen resolution.
		fullscreen true and resizable false are mandatory for Android.
	-->
	<window if="mobile" orientation="landscape" fullscreen="true" width="0" height="0" resizable="false" />
 
	<!-- Switch -->
	<window if="switch" orientation="landscape" fullscreen="true" width="0" height="0" resizable="true" />
 
	<!-- __________________________ Path Settings ___________________________ -->
 
	<set name="BUILD_DIR" value="export/debug"   if="debug" />
	<set name="BUILD_DIR" value="export/release" unless="debug" />
	<set name="BUILD_DIR" value="export/32bit"   if="32bits" />
 
	<classpath name="source" />
 
	<!-- _______________________ Game Assets & Etc. _________________________ -->
 
	<!-- Preload everything except on web -->
	<define name="PRELOAD_ALL"    unless="web" />
	<define name="NO_PRELOAD_ALL" unless="PRELOAD_ALL" />
 
	<assets path="assets/fonts" />
 
	<assets path="assets/preload" rename="assets" exclude="*.ogg" if="web" />
	<assets path="assets/preload" rename="assets" exclude="*.mp3" unless="web" />
 
	<section if="TITLE_SCREEN_EASTER_EGG">
		<assets path="assets/secrets" rename="assets" exclude="*.ogg" if="web" />
		<assets path="assets/secrets" rename="assets" exclude="*.mp3" unless="web" />
	</section>
 
	<define name="CHECK_FOR_UPDATES" unless="LOCAL" />
 
	<!-- Week Assets -->
	<library name="videos"       preload="true" />
 
	<section if="PRELOAD_ALL">
		<library name="songs"       preload="true" />
		<library name="shared"      preload="true" />
		<library name="week_assets" preload="true" />
	</section>
 
	<section if="NO_PRELOAD_ALL">
		<library name="songs"       preload="false" />
		<library name="shared"      preload="false" />
		<library name="week_assets" preload="false" />
	</section>
 
	<assets path="assets/videos"      library="videos"      if="VIDEOS_ALLOWED" />
 
	<assets path="assets/songs"       library="songs"       exclude="*.ogg" if="web" />
	<assets path="assets/songs"       library="songs"       exclude="*.mp3" unless="web" />
 
	<assets path="assets/shared"      library="shared"      exclude="*.ogg" if="web" />
	<assets path="assets/shared"      library="shared"      exclude="*.mp3" unless="web" />
 
	<assets path="assets/week_assets" library="week_assets" rename="assets" exclude="*.ogg" if="web" />
	<assets path="assets/week_assets" library="week_assets" rename="assets" exclude="*.mp3" unless="web" />
 
	<!--
		MODS:
		- Desktop/HTML5: embed example_mods from repo as the 'mods' folder (embed=false = loose files).
		- Mobile: do NOT bundle mods in the APK. The app will read from the external data folder
		  at runtime (see MODS_DATA_FOLDER note above).
		  The first-run code in your game should copy a default mod skeleton to
		  lime.system.System.applicationStorageDirectory + "mods/" if it doesn't exist yet.
	-->
	<section if="MODS_ALLOWED">
		<section unless="mobile">
			<assets path="example_mods" rename="mods" embed="false" />
		</section>
	</section>
 
	<!-- __________________________ Libraries ______________________________ -->
 
	<haxelib name="flixel" />
	<haxelib name="flixel-ui" />
	<haxelib name="flixel-addons" />
 
	<!-- Psych Engine extras -->
	<haxelib name="linc_luajit"   if="LUA_ALLOWED" />
	<haxelib name="SScript"       if="HSCRIPT_ALLOWED" />
	<haxelib name="hxvlc"         if="VIDEOS_ALLOWED" />
	<haxelib name="hxdiscord_rpc" if="DISCORD_ALLOWED" />
	<haxelib name="tjson" />
	<haxelib name="colyseus" />
	<haxelib name="HtmlParser" />
	<haxelib name="UnRAR" />
	<haxelib name="away3d" />
	<haxelib name="json2object" />
	<haxelib name="hxjsonast" />
	<haxelib name="flxanimate" />
	<haxelib name="lumod" />
	<haxelib name="actuate" />
	<haxelib name="compiletime" />
	<haxelib name="funkin.vis" />
	<haxelib name="grig.audio" />
	<haxelib name="yagp" />
	<haxelib name="tink_await" />
 
	<!-- Debug server (desktop/Android debug only — skip on release mobile to cut size) -->
	<haxelib name="hxcpp-debug-server" if="debug" unless="mobile" />
 
	<!-- _________________________ Haxe Defines ____________________________ -->
 
	<haxedef name="interpretable" />
	<haxedef name="interpret_watch" />
	<haxedef name="openflPos" />
	<haxedef name="hscriptPos" />
 
	<haxedef name="HXC_LIBVLC_LOGGING" if="VIDEOS_ALLOWED debug" />
	<haxedef name="LUMOD_HSCRIPT" />
 
	<!-- Avoid memory leaks with for loops in HScript -->
	<haxedef name="loop_unroll_max_cost" value="25" />
 
	<!-- Android-specific macro fix (required for Psych macros on Android) -->
	<haxedef name="DISABLED_MACRO_SUPERLATIVE" if="android" />
 
	<!-- Crash handler defines -->
	<haxedef name="HXCPP_CHECK_POINTER" />
	<haxedef name="HXCPP_STACK_LINE" />
	<haxedef name="HXCPP_STACK_TRACE" />
	<haxedef name="HXCPP_CATCH_SEGV" />
	<haxedef name="HXCPP_DEBUG_LINK" />
 
	<!-- Misc -->
	<haxedef name="FLX_NO_FOCUS_LOST_SCREEN" />
	<haxedef name="HXC_DEBUG_TRACE"     if="debug" />
	<haxedef name="FLX_NO_DEBUG" />
	<haxedef name="NAPE_RELEASE_BUILD"  unless="debug" />
	<haxedef name="no-deprecation-warnings" />
 
	<!-- Macro flags -->
	<haxeflag name="--macro" value="allowPackage('flash')" />
	<haxeflag name="--macro" value="include('my.pack')" />
	<haxeflag name="-D"      value="HXCPP_DEBUGGER" if="debug" />
	<haxeflag name="-debug"                          if="debug" />
 
	<section if="debugmon">
		<haxeflag name="--macro" value="addGlobalMetadata('online',   '@:build(online.backend.MonitorMacro.build())')" />
		<haxeflag name="--macro" value="addGlobalMetadata('states',   '@:build(online.backend.MonitorMacro.build())')" />
		<haxeflag name="--macro" value="addGlobalMetadata('flixel',   '@:build(online.backend.MonitorMacro.build())')" />
		<haxeflag name="--macro" value="addGlobalMetadata('backend',  '@:build(online.backend.MonitorMacro.build())')" />
		<haxeflag name="--macro" value="addGlobalMetadata('io',       '@:build(online.backend.MonitorMacro.build())')" />
		<haxeflag name="--macro" value="addGlobalMetadata('objects',  '@:build(online.backend.MonitorMacro.build())')" />
		<haxeflag name="--macro" value="addGlobalMetadata('psychlua', '@:build(online.backend.MonitorMacro.build())')" />
		<haxeflag name="--macro" value="addGlobalMetadata('llua',     '@:build(online.backend.MonitorMacro.build())')" />
	</section>
 
	<!-- ______________________________ Icons ______________________________ -->
 
	<assets path="art/iconOG.png" rename="icon.png" if="linux" />
	<icon path="art/icon16.png"  size="16" />
	<icon path="art/icon32.png"  size="32" />
	<icon path="art/icon64.png"  size="64" />
	<icon path="art/iconOG.png" />
 
</project>